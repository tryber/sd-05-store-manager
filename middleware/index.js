const { validateProductId, validateSaleId } = require('./validateId');
const validateProduct = require('./validateProduct');
const validateSale = require('./validateSale');

module.exports = { validateProductId, validateSaleId, validateProduct, validateSale };
