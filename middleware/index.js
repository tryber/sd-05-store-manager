const validateId = require('./validateId');
const validateProduct = require('./validateProduct');
const validateSale = require('./validateSale');

module.exports = { validateId, validateProduct, validateSale };
